{
  "name": "focus-pomodoro-extension",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "check": "node --check background.js && node --check popup.js && node --check options.js && node --check focus-analytics.js && node --check dashboard.js && node --check diagnostics.js"
  },
  "devDependencies": {
    "esbuild": "latest"
  }
}

